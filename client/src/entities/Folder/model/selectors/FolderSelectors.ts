import { StateSchema } from 'app/providers/StoreProvider';

// export const getFolderData = (state: StateSchema) => state.$?.data;
// export const getFolderIsLoading = (state: StateSchema) => state.$?.isLoading;
// export const getFolderError = (state: StateSchema) => state.$?.error;
